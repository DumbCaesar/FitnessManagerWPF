<Window x:Class="FitnessManagerWPF.View.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FitnessManagerWPF.View"
        xmlns:viewModel="clr-namespace:FitnessManagerWPF.ViewModel"
        xmlns:adminViewModel="clr-namespace:FitnessManagerWPF.ViewModel.Admin"
        xmlns:userControl="clr-namespace:FitnessManagerWPF.View.UserControl.Admin"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Admin Panel" Height="450" Width="800">
    <Window.DataContext>
        <viewModel:AdminViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type adminViewModel:AdminDashboardViewModel}">
            <userControl:AdminDashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type adminViewModel:AdminClassesViewModel}">
            <userControl:AdminClassesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type adminViewModel:AdminMemberViewModel}">
            <userControl:AdminMemberView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type adminViewModel:AdminTrainerViewModel}">
            <userControl:AdminTrainerView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Grid.Row="1">
            <RadioButton Content="Dashboard" Style="{StaticResource RadioButtonStyle}" Command="{Binding DashboardCommand}" IsChecked="True"/>
            <RadioButton Content="Members" Style="{StaticResource RadioButtonStyle}" Command="{Binding MemberCommand}"/>
            <RadioButton Content="Trainers" Style="{StaticResource RadioButtonStyle}" Command="{Binding TrainerCommand}"/>
            <RadioButton Content="Classes" Style="{StaticResource RadioButtonStyle}" Command="{Binding ClassesCommand}"/>
        </StackPanel>
        <ContentControl Grid.Column="1" Grid.Row="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>
