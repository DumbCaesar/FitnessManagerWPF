<Window x:Class="FitnessManagerWPF.View.Admin.SelectedClassView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FitnessManagerWPF.View.Admin"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="SelectedClassView" Height="450" Width="590">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Class: " Margin="10,0,0,0"/>
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding GymClasses}" SelectedItem="{Binding SelectedClass}" DisplayMemberPath="Name" Margin="0,0,10,5"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Trainer: " Margin="10,0,0,0"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedClass.Trainer.Name}" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Day: " Margin="10,0,0,0"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedClass.Day}" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Time: " Margin="10,0,0,0"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedClass.Time}" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Limit: " Margin="10,0,0,0"/>
        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedClass.MaxParticipants}" Margin="0,0,0,10"/>

        <ListView Grid.Row="5" Grid.ColumnSpan="2" ItemsSource="{Binding ListOfUsers}" SelectedItem="{Binding SelectedMember}" Margin="10">
            <ListView.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource HorizontalCleanScrollBar}" />
            </ListView.Resources>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding MemberDoubleClickCommand}" CommandParameter="{Binding SelectedMember}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView.View>
                <GridView AllowsColumnReorder="False">
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="70"/>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="180"/>
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="200"/>
                    <GridViewColumn Header="Membership" DisplayMemberBinding="{Binding MembershipStatusDisplay}" Width="140"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Grid.Row="6" Grid.Column="1" Content="Delete"  Width="80" Height="28" Margin="0,0,8,0" HorizontalAlignment="Right"  Background="#EF4444" Foreground="White" FontWeight="Medium" Cursor="Hand" Command="{Binding DeleteClassCommand}"/>
    </Grid>
</Window>
