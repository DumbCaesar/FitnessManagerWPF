<Window x:Class="FitnessManagerWPF.View.Admin.EditMemberView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FitnessManagerWPF.View.Admin"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Edit Member" Height="320" Width="400"
        Background="#FAFAFA">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Name -->
        <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8" Foreground="#1F2937"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,4,0,8" Height="32" Padding="8,0" VerticalContentAlignment="Center" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Email -->
        <TextBlock Text="Email:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8" Foreground="#1F2937"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,4,0,8" Height="32" Padding="8,0" VerticalContentAlignment="Center" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Login -->
        <TextBlock Text="Login:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8" Foreground="#1F2937"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,4,0,8" Height="32" Padding="8,0" VerticalContentAlignment="Center" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Password -->
        <TextBlock Text="Password:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8" Foreground="#1F2937"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="0,4,0,8" Height="32" Padding="8,0" VerticalContentAlignment="Center" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="Discard" Width="80" Height="32" Margin="0,0,8,0" Background="White" Foreground="#6B7280" BorderBrush="#D1D5DB" BorderThickness="1" Cursor="Hand" Command="{Binding DiscardCommand}" IsCancel="True"/>
            <Button Content="Save Changes" Width="110" Height="32" Background="#10B981" Foreground="White" FontWeight="Medium" Cursor="Hand" Command="{Binding SaveCommand}" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>