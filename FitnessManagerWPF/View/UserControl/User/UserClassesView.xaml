<UserControl x:Class="FitnessManagerWPF.View.UserControl.User.UserClassesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessManagerWPF.View.UserControl.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Classes}" Margin="20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" Background="WhiteSmoke" Padding="15" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Width="250">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="28" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="Trainer" FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Trainer.Name}" FontSize="28" FontWeight="Bold"/>
                                <TextBlock Text="Participants" FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding CapacityDisplay}" FontSize="28" FontWeight="Bold"/>
                                <Button FontSize="14" Command="{Binding DataContext.SignUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Height="30">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Content" Value="Sign up"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUserEnrolled}" Value="True">
                                                    <Setter Property="Content" Value="Cancel"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
