<UserControl x:Class="FitnessManagerWPF.View.UserControl.User.UserClassesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessManagerWPF.View.UserControl.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid Background="#F5F5F5">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <!-- Header -->
                <TextBlock Text="Membership" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                <ItemsControl ItemsSource="{Binding Classes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="10" Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Width="260">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding TimeDisplay}" FontSize="12" FontWeight="Bold" Foreground="#2C3E50" TextTrimming="CharacterEllipsis" Margin="0 0 0 20"/>
                                    <TextBlock Text="Trainer" FontSize="12" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                    <TextBlock Text="{Binding Trainer.Name}" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                                    <TextBlock Text="Participants" FontSize="12" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0 10 0 0"/>
                                    <TextBlock Text="{Binding CapacityDisplay}" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                                    <Button FontSize="14" Command="{Binding DataContext.SignUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Height="42" Cursor="Hand" Margin="0 20 0 0">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Content" Value="Sign Up"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="Background" Value="#FF8C42"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                                <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsUserEnrolled}" Value="True">
                                                        <Setter Property="Content" Value="Cancel"/>
                                                    </DataTrigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#FF7A28"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background" Value="#E66A20"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
