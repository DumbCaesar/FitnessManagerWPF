<UserControl x:Class="FitnessManagerWPF.View.UserControl.User.UserMembershipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessManagerWPF.View.UserControl.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
            <ComboBox ItemsSource="{Binding ListOfMemberships}" SelectedItem="{Binding SelectedMembership}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Content="Buy" Width="50" Margin="5" Command="{Binding BuyMembershipCommand}"/>
        </StackPanel>
        <ListBox Grid.Column="1" ItemsSource="{Binding UserSubscriptions}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Id, StringFormat='Subscription ID: {0}'}"/>
                        <TextBlock Text="{Binding StartDate, StringFormat='Subscription Start: {0:hh:mm tt dd/MM/yyyy}'}"/>
                        <TextBlock Text="{Binding EndDate, StringFormat='Subscription Start: {0:hh:mm tt dd/MM/yyyy}'}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
