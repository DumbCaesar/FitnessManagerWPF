<UserControl x:Class="FitnessManagerWPF.View.UserControl.User.UserMembershipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessManagerWPF.View.UserControl.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid Background="#F5F5F5">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">
            <!-- Header -->
            <TextBlock Text="Membership" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                <ItemsControl ItemsSource="{Binding ListOfMemberships}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="10" Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Width="230">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" TextTrimming="CharacterEllipsis" Margin="0,0,0,10"/>
                                    <TextBlock Text="Price" FontSize="12" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,0"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding AmountDisplay}" FontSize="32" FontWeight="Bold" Foreground="#2C3E50"/>
                                    </StackPanel>

                                    <TextBlock Text="Duration" FontSize="12" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,10,0,0"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding DurationInMonths}" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
                                        <TextBlock Text=" months" FontSize="16" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,10" VerticalAlignment="Bottom"/>
                                    </StackPanel>

                                    <Button FontSize="14" Command="{Binding DataContext.BuyMembershipCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Height="42" Cursor="Hand" Margin="0 20 0 0">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Content" Value="Buy Membership"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="Background" Value="#FF8C42"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                                <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActiveType}" Value="True">
                                                        <Setter Property="Content" Value="Extend Membership"/>
                                                    </DataTrigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#FF7A28"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background" Value="#E66A20"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
