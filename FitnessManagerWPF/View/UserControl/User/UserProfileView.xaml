<UserControl x:Class="FitnessManagerWPF.View.UserControl.User.UserProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessManagerWPF.View.UserControl.User"
             xmlns:helper="clr-namespace:FitnessManagerWPF.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Black" Grid.RowSpan="2" BorderThickness="1" Margin="2" CornerRadius="12" VerticalAlignment="Top" Height="340" Background="WhiteSmoke">
            <StackPanel Grid.Column="0" Margin="2">
                <TextBlock Text="{Binding DateJoined, StringFormat='Member since: {0:dd/MM/yyyy}'}" Margin="2 0 0 0"/>
                <TextBlock Text="{Binding MembershipTypeDisplay, StringFormat='Status: {0}'}" Margin="2 2 0 0"/>
                <Separator Margin="2 2 0 0" Width="195" HorizontalAlignment="Left" Background="Black"/>
                <TextBlock Text="Name:" Margin="2"/>
                <Border BorderBrush="Black" Background="FloralWhite" BorderThickness="1" CornerRadius="8" Width="200" HorizontalAlignment="Left">
                    <TextBox Margin="2" BorderThickness="0" Background="Transparent" Text="{Binding Name}"/>
                </Border>
                <TextBlock Text="Email:" Margin="2"/>
                <Border BorderBrush="Black" Background="FloralWhite" BorderThickness="1" CornerRadius="8" Width="200" HorizontalAlignment="Left">
                    <TextBox Margin="2" BorderThickness="0" Background="Transparent" Text="{Binding Email}"/>
                </Border>
                <TextBlock Text="Login:" Margin="2"/>
                <Border BorderBrush="Black" Background="FloralWhite" BorderThickness="1" CornerRadius="8" Width="200" HorizontalAlignment="Left">
                    <TextBox Margin="2" BorderThickness="0" Background="Transparent" Text="{Binding Username}"/>
                </Border>
                <TextBlock Text="Current Password:" Margin="2"/>
                <Border BorderBrush="Black" Background="FloralWhite" BorderThickness="1" CornerRadius="8" Width="200" HorizontalAlignment="Left">
                    <TextBox Margin="2" BorderThickness="0" helper:PasswordBoxHelper.PasswordChar="True" helper:PasswordBoxHelper.ActualPassword="{Binding Password}" Background="Transparent"/>
                </Border>
                <TextBlock Text="New Password:" Margin="2"/>
                <Border BorderBrush="Black" Background="FloralWhite" BorderThickness="1" CornerRadius="8" Width="200" HorizontalAlignment="Left">
                    <TextBox Margin="2" BorderThickness="0" helper:PasswordBoxHelper.PasswordChar="True" helper:PasswordBoxHelper.ActualPassword="{Binding NewPassword}" Background="Transparent"/>
                </Border>
                <TextBlock Text="New Password:" Margin="2"/>
                <Border BorderBrush="Black" Background="FloralWhite" BorderThickness="1" CornerRadius="8" Width="200" HorizontalAlignment="Left">
                    <TextBox Margin="2" BorderThickness="0" helper:PasswordBoxHelper.PasswordChar="True" helper:PasswordBoxHelper.ActualPassword="{Binding NewPasswordCompare}" Background="Transparent"/>
                </Border>
                <Grid Margin="2 5 0 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="50" BorderThickness="1" BorderBrush="Black" CornerRadius="8" Padding="2" Background="#63bf3c">
                        <Button Height="20" Width="50" Content="Save" BorderThickness="0" Background="Transparent" Style="{StaticResource FlatButton}" Command="{Binding SaveCommand}" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="1" Margin="2 0 0 0" Width="50" BorderThickness="1" BorderBrush="Black" CornerRadius="8" Padding="2" Background="#DC143C">
                        <Button Height="20" Width="50" Content="Discard" BorderThickness="0" Background="Transparent" Style="{StaticResource FlatButton}" Command="{Binding DiscardCommand}" HorizontalAlignment="Center"/>
                    </Border>

                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
