<UserControl x:Class="FitnessManagerWPF.View.UserControl.User.UserProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessManagerWPF.View.UserControl.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Margin="2" Grid.RowSpan="2">
            <TextBlock Text="Name:" Margin="2"/>
            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="8" Width="200" HorizontalAlignment="Left">
            <TextBox Margin="2" BorderThickness="0" Text="{Binding Name}"/>
            </Border>
            <TextBlock Text="Email:" Margin="2"/>
            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="8" Width="200" HorizontalAlignment="Left">
                <TextBox Margin="2" BorderThickness="0" Text="{Binding Email}"/>
            </Border>
            <TextBlock Text="Login:" Margin="2"/>
            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="8" Width="200" HorizontalAlignment="Left">
                <TextBox Margin="2" BorderThickness="0" Text="{Binding Username}"/>
            </Border>
            <TextBlock Text="Password:" Margin="2"/>
            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="8" Width="200" HorizontalAlignment="Left">
                <TextBox Margin="2" BorderThickness="0" Text="{Binding Password}"/>
            </Border>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Height="20" Width="50" Content="Save" Command="{Binding SaveCommand}"/>
                <Button Grid.Row="1" Height="20" Width="50" Content="Discard" Command="{Binding DiscardCommand}"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
