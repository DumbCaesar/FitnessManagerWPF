<UserControl x:Class="FitnessManagerWPF.View.UserControl.Admin.AdminTrainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessManagerWPF.View.UserControl.Admin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid Margin="16" Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,5,16">
            <TextBlock Text="Trainer Overview" FontSize="20" FontWeight="SemiBold" Foreground="#1F2937" VerticalAlignment="Center"/>
            <Button Content="New Trainer" Width="120" Height="36" Background="#10B981" Foreground="White" FontWeight="Medium" HorizontalAlignment="Right" Cursor="Hand" Margin="0 5 0 0"/>
        </DockPanel>

        <!-- Trainers -->
        <ListView Grid.Row="1" ItemsSource="{Binding TrainerList}" SelectedItem="{Binding SelectedTrainer}" Margin="5">
            <ListView.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource HorizontalCleanScrollBar}" />
            </ListView.Resources>
            <ListView.View>
                <GridView AllowsColumnReorder="False">
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="70"/>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="180"/>
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
