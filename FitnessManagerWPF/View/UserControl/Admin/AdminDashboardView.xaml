<UserControl x:Class="FitnessManagerWPF.View.UserControl.Admin.AdminDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessManagerWPF.View.UserControl.Admin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Margin="5" Background="WhiteSmoke" Padding="15" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Height="220" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Total members" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TotalMemberCount}" FontSize="32" FontWeight="Bold"/>
                <TextBlock Text="Active memberships" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ActiveMemberCount}" FontSize="32" FontWeight="Bold"/>
                <TextBlock Text="Inactive memberships" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding InactiveMemberCount}" FontSize="32" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Margin="5" Background="WhiteSmoke" Padding="15" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Height="220" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Classes today" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ClassesToday}" FontSize="32" FontWeight="Bold"/>
                <TextBlock Text="Classes this week" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ClassesWeek}" FontSize="32" FontWeight="Bold"/>
                <TextBlock Text="Total attendance this week" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Attendance}" FontSize="32" FontWeight="Bold"/>
            </StackPanel>
        </Border>
        <Border Grid.Column="2" Margin="5" Background="WhiteSmoke" Padding="15" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Height="220" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Monthly Recurring Revenue" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding MRR, StringFormat='Kr {0:N0}/mnd'}" FontSize="32" FontWeight="Bold"/>
                <TextBlock Text="New sign-ups this month" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding MonthlySignups}" FontSize="32" FontWeight="Bold"/>
                <TextBlock Text="Memberships expiring next 7 days" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ExpiringMemberships}" FontSize="32" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
