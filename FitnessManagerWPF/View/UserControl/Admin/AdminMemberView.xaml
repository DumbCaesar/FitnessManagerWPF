<UserControl x:Class="FitnessManagerWPF.View.UserControl.Admin.AdminMemberView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessManagerWPF.View.UserControl.Admin"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid>
        <ListView Grid.Row="1" ItemsSource="{Binding MemberList}" SelectedItem="{Binding SelectedMember}" Margin="5">
            <ListView.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource HorizontalCleanScrollBar}" />
            </ListView.Resources>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding MemberDoubleClickCommand}" CommandParameter="{Binding SelectedMember}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView.View>
                <GridView AllowsColumnReorder="False">
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="70"/>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="180"/>
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="200"/>
                    <GridViewColumn Header="Membership" DisplayMemberBinding="{Binding MembershipTypeDisplay}" Width="140"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
